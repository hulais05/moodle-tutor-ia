# El entorno de Moodle + Docker NO va al repo (es pesado y no es código propio).
# Vive aparte, en ~/moodle-dev/ — ver docs/como-reproducir.md
moodle-docker/
moodle/

# Sistema
.DS_Store
*.log

# Temporales
_provision/
*.tmp

# Informe generado al correr el script (el ejemplo curado está en docs/)
scripts/informe_alumnos.md
